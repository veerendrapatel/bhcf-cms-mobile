import { schoolStatusConstants } from '../constants';


const initState = {
    items: null,
    error: null,
    loading: false
}
const schoolStatusReducer = (state = initState, action) => {
   return state;
}

export default schoolStatusReducer;
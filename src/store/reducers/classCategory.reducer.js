import { classCategoryConstants } from '../constants';


const initState = {
    items: null,
    error: null,
    loading: false
}
const classCategoryReducer = (state = initState, action) => {
   return state;
}

export default classCategoryReducer;